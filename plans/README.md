# Implementation Plans

Generated by the `improve` skill on 2026-06-13 against the working tree at commit
`a3682db` (plus uncommitted session changes to `event.dart`,
`adminattendencepage.dart`, `admineventspage.dart`, `adminlistspage.dart`,
`customeventformpage.dart`, `homescreenpage.dart`). Each plan stamps that
baseline and includes a drift check.

App: Wheeler NHS Hour Tracker (`nhs_tracker`) — Flutter + Supabase, Provider
state. **No automated test suite** (`test/widget_test.dart` is empty) and **no
CI**, so every plan uses `flutter analyze` as its verification gate plus manual
checks. The Flutter binary is at `/Users/cfournier/Flutter SDK/flutter/bin/flutter`
if `flutter` isn't on PATH (the path contains a space — quote it).

Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix null-`hours` precedence in hour totals | P1 | S | — | TODO |
| 002 | Pass `societyId` on signup activity log | P1 | S | — | TODO |
| 003 | Make time-slot signup atomic (no oversell) | P1 | M | — | TODO |
| 004 | Replace `print()` with `debugPrint()` | P2 | M | 001,002,003 | TODO |
| 005 | Consolidate admin event-fetch into one path | P2 | M | — (before 008) | TODO |
| 006 | Swap-request inbox on home screen | P2 | M | — (before 008) | TODO |
| 007 | Schedule local event reminders on signup | P2 | M | 003 (before 008) | TODO |
| 008 | Decompose god-files — extract home progress bars (slice 1) | P3 | M | 001,002,003,006,007 | TODO |
| 009 | UI consistency — align all screens to the Attendance design | P2 | L | — (coordinate with 008 on home) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **004** edits `homescreenpage.dart`/`completedhourspage.dart`, which 001/002/003
  also touch. Land 001–003 first so 004's mechanical `print` sweep doesn't fight
  those edits.
- **003 → 007**: Plan 003 rewrites the signup success block; 007 inserts reminder
  scheduling into that same block. Land 003 first (007 anchors on
  `hapticsProvider.success();`, which survives either way, but ordering avoids
  churn).
- **008 runs LAST.** It edits `homescreenpage.dart`, which 001, 002, 003, 006, and
  007 all modify. Doing it last avoids repeated conflicts.
- 005 and 006 are independent of each other but should both precede 008 (008's
  later backlog slices touch the same files).
- 009 (UI consistency) is independent of the correctness/feature plans but
  overlaps `homescreenpage.dart` with 006/008 — do 009's Home batch before 008's
  extraction, or re-check after, so extracted widgets carry the standardized
  chips/cards.

## Suggested grouping for execution

- **Quick correctness wins (low risk):** 001, 002 — small, isolated, safe to do together.
- **Core bug (needs a DB migration applied by the operator):** 003.
- **Hygiene:** 004.
- **Maintainability:** 005, then 008-slice-1.
- **Features (already-built infra):** 006 (swap inbox), 007 (event reminders).

## Findings considered and rejected

- **"Build push notifications" (original direction item): mostly already built.**
  The repo has `NotificationService`, `FirebaseMessagingService`, device-token
  sync, a `supabase/functions/send-push/` edge function, DB triggers in
  `20260526000003_notification_triggers.sql` (swap requests, meeting notes, hour
  updates, continuous events), and native Firebase config files. The only real
  client gap was that `scheduleEventReminder` is never called — that became Plan
  007. No greenfield push build is warranted.
- **Hardcoded Supabase anon key in `main.dart`: by design.** It is the public
  anon key, documented as intentional in `CLAUDE.md`. Not a finding.
- **`BuildContext` used across async gaps (pervasive analyzer info):** benign
  given the dialog/snackbar usage patterns; not worth a dedicated plan. Address
  opportunistically (some plans add `mounted` guards in passing).
- **Unbounded list fetches (home/admin load all events + slots + attendees):**
  fine at NHS-chapter scale; revisit only if societies grow large. Not worth doing now.

## Not selected this round (available to plan later)

- **Verification baseline (tests + CI):** the highest-leverage *unblocker* — a
  34K-LOC shipping app with no tests and no CI. Every plan here leans on `flutter
  analyze` only because of this gap. Strongly recommended as the next thing to plan.
- **Authorization / RLS verification (security):** destructive admin actions
  (delete events, bulk-delete service hours) are gated client-side by `isAdmin`;
  whether Supabase RLS enforces society-scoped admin writes server-side could not
  be verified from the repo. Worth an investigation plan: audit RLS policies on
  `Events`, `"Service hours"`, `Attendees`, `swap_requests`.
- **Error-handling pass:** ~126 `catch` blocks mostly just log; wiring a crash
  reporter (Firebase Crashlytics — `firebase_core` already present) and surfacing
  user-facing errors on critical paths is a good follow-up to Plan 004.
